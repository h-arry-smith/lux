program = _{ SOI ~ "\n"* ~ (stmt ~ "\n"+)* ~ stmt? ~ EOI }
stmt = { select | apply }

select = { query ~ "{" ~ "\n"* ~ (stmt ~ "\n"+)*  ~ "}"}
query = { numeric } 

apply = { ident ~ ":" ~ generator }
generator = { numeric }

ident = @{ ASCII_ALPHA ~ (ASCII_ALPHANUMERIC | "_")* }
numeric = @{ ("+" | "-")? ~ ASCII_DIGIT+ ~ ("." ~ ASCII_DIGIT+)? }

WHITESPACE = _{ " " | "\t" }
